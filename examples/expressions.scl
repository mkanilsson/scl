extern proc printf(string, ...) i32

struct Vec {
    x: u32,
}

proc vecadd(v1: Vec, v2: Vec) Vec {
    ret Vec {
        x: v1.x + v2.x,
    };
}

proc main() i32 {
    let v = Vec {
        x: 15,
    };

    let result = vecadd(v, Vec {
        x: 50,
    });

    printf("%d\n", result.x);
    ret 0;
}
