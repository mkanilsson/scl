extern proc printf(string, ...) i32

struct Vec {
    x: u32,
    y: u32
}

proc main() i32 {
    let v = Vec {
        x: 34,
        y: 35
    };
    
    printf("x: %d\n", v.y + v.x);
    ret 0;
}
