use std::io::printf

interface Print {
    proc print(this) void  
}

struct Thing {
    a: i32
}

impl Print for Thing {
    proc print(this) void {
        printf("Value: %d\n", this.a);
        ret;
    } 
}

proc main() void {
    ret;
}
